/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ['vi', 'en'],
    defaultLocale: 'vi',
    localeDetection: false,
  },
  reactStrictMode: false,
  env: {},
  images: {},
}

export default nextConfig
